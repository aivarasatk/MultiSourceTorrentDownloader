<Window x:Class="MultiSourceTorrentDownloader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:model="clr-namespace:MultiSourceTorrentDownloader.Models"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="15"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="App"
        Width="1000" Height="900"
        d:DataContext="{d:DesignInstance d:Type=model:MainModel}">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.DialogHost.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <materialDesign:DialogHost Identifier="RootDialog"> <!-- to show in-window popups-->
        <Grid>
            <Grid.Resources>
                <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            </Grid.Resources>

            <Grid>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Margin="16" Header="Search Criteria">
                    <Grid FocusManager.FocusedElement="{Binding ElementName=SearchBox}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Grid.Row="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="SearchBox"  Grid.Row="0" Grid.Column="0" Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged}" Height="auto" Margin="0,0,16,0" 
                                 materialDesign:HintAssist.Hint="Search for torrents.." ToolTip="Pressing enter will activate search">
                                <TextBox.InputBindings>
                                    <KeyBinding Command="{Binding SearchCommand}" Key="Enter" />
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button  Grid.Row="0" Grid.Column="1" Command="{Binding SearchCommand}" Content="Search"/>

                            <ComboBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Filters}" SelectedItem="{Binding SelectedFilter}" DisplayMemberPath="Value" Margin="0,16,0,0" HorizontalAlignment="Left"/>
                        </Grid>

                        <Grid Grid.Row="1" Margin="0,32,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <GroupBox  Header="Sources" Width="auto">
                                <StackPanel Orientation="Horizontal" Margin="8">
                                    <ToggleButton Style="{DynamicResource MaterialDesignFlatPrimaryToggleButton}" FontSize="14" Content="The Pirate Bay" Width="100" IsChecked="{Binding ThePirateBaySourceSelected}"/>
                                    <ToggleButton Style="{DynamicResource MaterialDesignFlatPrimaryToggleButton}" FontSize="14" Content="1337X" Width="100" IsChecked="{Binding LeetxSelected}" Margin="16,0,0,0"/>
                                </StackPanel>
                            </GroupBox>
                        </Grid>
                        
                    </Grid>
                    
                </GroupBox>

                <GroupBox Grid.Row="1" Margin="16" Header="Torrents">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <DataGrid Grid.Row="0" ItemsSource="{Binding TorrentEntries}" IsReadOnly="True" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Title" FontWeight="Bold" Binding="{Binding Title}"/>
                                <DataGridTextColumn Header="Seeders" Foreground="Green" Binding="{Binding Seeders}"/>
                                <DataGridTextColumn Header="Leechers" Foreground="Red" Binding="{Binding Leechers}"/>
                                <DataGridTextColumn Header="Time" Foreground="Black" Binding="{Binding Date}"/>
                                <DataGridTextColumn Header="Size" Foreground="Black" Binding="{Binding Size}"/>
                                <DataGridTextColumn Header="Uploader" Foreground="Black" Binding="{Binding Uploader}"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Button Grid.Row="1" Command="{Binding LoadMoreCommand}" Content="Load more" Margin="0,16,0,0" Width="120" HorizontalAlignment="Left"
                                ToolTip="Loads more data from next torrent page"/>
                    </Grid>
                    
                </GroupBox>
                
                <TextBlock Grid.Row="2" Margin="16"/>
            </Grid>
            
            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Background="LightGray" Opacity="0.5"/> <!-- prevents user clicks-->
                <fa:ImageAwesome Icon="Spinner" Spin="True" SpinDuration="2" Width="40" Height="40" Visibility="Visible" Margin="16,16,16,16"/>
            </Grid>
        </Grid>
    </materialDesign:DialogHost>
</Window>
